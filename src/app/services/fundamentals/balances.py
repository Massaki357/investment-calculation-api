"""Balance-sheet helpers."""


def average_balance(beginning: float, ending: float) -> float:
    """Simple average of beginning and ending balances: (beginning + ending) / 2."""
    return (beginning + ending) / 2
